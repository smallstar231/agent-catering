# ==============================================
# LangChain ReAct Agent - Smart Customer Service
# Install: pip install -r requirements.txt
# ==============================================

# LangChain core
langchain>=1.0
langchain-core>=1.0
langchain-community>=0.4

# Vector database
langchain-chroma>=1.0
chromadb>=1.0
langchain-text-splitters>=1.0

# Agent orchestration
langgraph>=1.0

# Frontend
streamlit>=1.40

# Document processing
pypdf>=5.1.0
pyyaml>=6.0.2
pymupdf>=1.28   # PDF 内嵌图片抽取（多模态图文 RAG）

# Alibaba Cloud DashScope model
dashscope>=1.20.14

# Env file loading (.env) + HTTP client (Tavily search / Jina page reader)
python-dotenv>=1.0
httpx>=0.27

# MCP (Model Context Protocol) server + client (mcp 1.x for FastMCP)
mcp>=1.30,<2
